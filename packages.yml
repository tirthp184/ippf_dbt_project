# No packages currently required. dbt_utils will be added here once a model
# actually needs it (planned: dim_date via date_spine, surrogate key helpers,
# reconciliation tests) - at that point we'll also set up the network rule +
# external access integration needed for `dbt deps` to reach the package hub.
#
# Deliberately left with an empty packages list (not omitted) so this file
# stays the single place to look when that need arises.
packages: []